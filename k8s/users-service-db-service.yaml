apiVersion: v1
kind: Service
metadata:
  name: users-service-db
  namespace: my-app-namespace
spec:
  selector:
    app: users-service-db
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
  type: ClusterIP